---
title: 'Introduction to Graph Learning'
date: 2026-05-11
permalink: /posts/2026/05/introduction-to-graph-learning/
tags:
  - Gaussian Graphical Models
  - Conditional Correlation
  - Graph Signal Processing
---

-----

Definition of graphs 
======

Graph
------

$$
\mathcal{G}=(\mathcal{V},\mathcal{E},\omega)
$$

- Nodes (vertices): $\mathcal{V}=\\{1,\dots,p\\}$, representing the variables.

- Edges:
$\mathcal{E}\subseteq\mathcal{V}\times\mathcal{V}$, representig the relationship between nodes.

- Weights: $\omega:\mathcal{E}\to\mathbb{R}$, for $(q,l)\in\mathcal{E}, \ \ w_{q,l}=\omega((q,l)), \ \ \mathbf{w}\in\mathbb{R}^{p(p-1)/2}$, representing the strong/weak relationship between nodes.
  
Matrix representations
-------

- **Adjacency matrix**

$$
[\mathbf A]_{q,\ell}=
\begin{cases}
w_{q,\ell}, & (q,\ell)\in\mathcal E,\\
0, & \text{otherwise.}
\end{cases}
$$

- **Graph Laplacian**
$$\mathbf{L}=\mathbf D-\mathbf{A},$$ and the degree matrix $$\mathbf{D}=\mathrm{diag}(\mathbf{A}\mathbf{1})$$ is a diagonal matrix whose entries contain the total connectivity (or weighted degree) of each node.

The role of Graphs in data analysis and data science
====

Graphs play an important role in both data analysis and data science. In data analysis, graphs are commonly used as tools for visualization (e.g., NetworkX, Pyvis) and for unsupervised graph clustering algorithms, such as spectral clustering [Ng et al., 2002a], modularity maximization [Newman et al., 2004; Hollocou et al., 2019], random-walk-based approaches [Pons et al., 2005], and label propagation [Cordasco et al., 2011]. Graphs are also widely used in Graph Signal Processing (GSP) for the design of graph filters and spectral representations [Shuman et al., 2013].

In data science, graph structures are leveraged to enrich node embeddings by combining the intrinsic information of each node with the topological information provided by the graph structure. This integration significantly improves the performance of downstream tasks, such as node classification, link prediction [Zhang et al., 2021].

The objective of graph learning 
=====

Graph learning aims to infer the underlying graph structure from observed data associated with a set of vertices. Assuming that each vertex is associated with $n$ observed values or signals, the objective is to recover the edge set that best represents the relationships between nodes. A major challenge in graph learning is the ill-posed nature of the problem, since many different graph structures may explain the same observations.

![]({{site.url}}/images/new_GL_obj.png)

How to define an edge between two nodes ?  
=====

Given a data matrix $\mathbf{X} = \\{ \mathbf{x}_1, \mathbf{x}_2, \ldots, \mathbf{x}_n \\} \in \mathbb{R}^{p \times n},$ containing $n$ observations, where each observation is defined as $\mathbf{x}_i = \[x_1, x_2, \ldots, x_p\]^\top, \ i \in \\{1,\ldots,n\\},\$ $p$ is the number of variables. 

Several definitions of the relationship between two vertices can be considered. A first approach defines edges according to the distance between the geometric embeddings of the nodes, where nearby embeddings indicate stronger relationships. More specifically, if the observations associated with each node are represented by an embedding vector, an edge can be established between two nodes $x_q$ and $x_l$ based on the distance between their corresponding embeddings $\mathbf{X}_q$ and $\mathbf{X}_l$, where $\mathbf{X}_p$ denotes the $p$-th row vector of the data matrix $\mathbf{X}$:

$$
d(\mathbf{X}_q,\mathbf{X}_l)
= \|\mathbf{X}_q-\mathbf{X}_l\|_2.$$

Another approach relies on conditional statistical dependencies between nodes under a given probabilistic model. In this setting, two nodes $x_1$ and $x_2$ are connected if

$$
P(x_1 \mid x_2, \text{other nodes}) \neq P(x_1 \mid \text{other nodes}),
$$

which means that knowing $x_2$ provides additional information about $x_1$, even after accounting for all the other nodes. Therefore, an edge is introduced between $x_1$ and $x_2$.


The framework of Graph Learning
=====

In this part, we decompose principle components of a graph learning framework in order to learn an underlying graph structure explaining the data observations $\mathbf{X} \in \mathbb{R}^{p \times n}$, $n$ is number of samples and $p$ is number of variables :

$$
\begin{aligned}
\underset{\boldsymbol{\Theta}}{\underbrace{\text{minimize}}_{\color{pink}\text{Opt. Solver}}}
\quad &
{\underbrace{\mathcal{L}(\boldsymbol{\Theta},\mathbf{X})}_{\color{magenta}\text{data-fitting term}}}
+
{\underbrace{\lambda h(\boldsymbol{\Theta})}_{\color{orange}\text{sparsity-promoting penalty term}}}
\\
\text{subject to}
\quad &
\underbrace{\boldsymbol{\Theta} \in \mathcal{S}_{\boldsymbol{\Theta}}}_{\color{green}\text{structured precision}}
\\
&
\underbrace{\boldsymbol{\Sigma} \in \mathcal{S}_{\boldsymbol{\Sigma}}}_{\color{cyan}\text{structured covariance}}
\text{and} \qquad
\boldsymbol{\Theta}^{+} = \boldsymbol{\Sigma}.
\end{aligned}
$$

+ $\color{magenta}\text{Data-fitting term}$: $\mathcal{L}(\boldsymbol{\Theta}, \mathbf{X})$ learns the graph structure directly from the data observations, which can be designed as a robust function to missing data, outliers, corrupted data, etc., or, the assumption of an underlying statistical model generating data observations can be applied, for example, the negative Gaussian log-likelihood function:
  
$$
\mathcal{L}(\mathbf{\Theta},\mathbf{X}) = {\rm tr(\mathbf{S}\mathbf{\Theta})}-{\rm log|\mathbf{\Theta}|}+{\rm const},
$$

<div style="margin-left: 40px;">
where $\mathbf{S} = \frac{1}{n}\mathbf{X}\mathbf{X}^{\top}, \ \mathbf{S} \in \mathbb{R}^{p \times p}$ is empirical sample covariance. This function sets a basic for various works, notably GLasso [Friedman et al, 2008] and Laplacian-GMRF [Gilmez et al., 2017]. Another option, e.g, Elliptical distribution, can be applied to model more general behaviors of data (heavy-tailed, contaminated by impulsive noise, interupted by significant outliers, etc.,). 
</div>

+ $\color{orange}\text{Sparsity-promoting penalty term}$: $h(\boldsymbol{\Theta})$, which goes with a regularization parameter $\lambda$, is commonly used in any graph learning algorithm, to promoting the sparsity structure in graph, since the underlying graph inferred from data observations is expected to contain zero correlations between nodes, instead of fully connections. Several options of the sparsity-penalty term can be used, notably $\ell_1$-norm, then non-convex penalties (SCAD, MCP, capped-$\ell_1$).
  
+ $\color{green}{\text{Structured precision}}$, represented by $\boldsymbol{\Theta}$, and $\color{cyan}{\text{structured covariance}}$, represented by $\boldsymbol{\Sigma}$. When $\boldsymbol{\Theta}$ is identified as the precision matrix, i.e., $\boldsymbol{\Theta} = \boldsymbol{\Sigma}^{\dagger},$ its support is commonly used to characterize the graph structure. In this setting, the precision matrix plays the role of the adjacency matrix, where an edge between two nodes is established whenever the corresponding coefficient exceeds a predefined threshold. Several structural constraints were directly imposed on the precision matrix $\boldsymbol{\Theta}$, including Symmetric Positive Definite (SPD), total positivity (MTP2), Laplacian constraints, and spectral constraints on the eigenvalues of the Laplacian precision matrix.
An alternative approach consists in imposing structural constraints on the covariance matrix $\boldsymbol{\Sigma}$, where $\boldsymbol{\Sigma} = \boldsymbol{\Theta}^{\dagger}.$ This formulation is commonly explored through factor models, in which the covariance matrix is decomposed into sparse and low-rank components.

+ $\color{pink} \text{Optimization Solver}$: Given the choice of models and structural constraints, graph learning problems are typically non-convex and therefore challenging to solve. Various optimization algorithms have been developed to address the corresponding graph learning formulations and structural constraints.In particular, iterative optimization methods, such as Block Coordinate Descent (BCD), Majorization-Minimization (MM), Block-MM, and proximal second-order methods, decompose the graph-representative matrix into line-wise or block-wise subproblems that can be solved more efficiently. Beyond these iterative schemes, when multiple structural constraints or non-smooth regularization terms are combined, a single optimization framework often becomes restrictive. In such settings, variable-splitting strategies, such as the Alternating Direction Method of Multipliers (ADMM), have proven particularly effective by decomposing the original optimization problem into a set of simpler and more tractable subproblems. Another work proposed to leverage the manifolds for the graph structures, and then used Riemannian Optimization to solve the problem. 

Several output examples of Graph Learning's algorithms:
=======

<div align="center">

<table>
<tr>
<td align="center">
<img src="{{site.url}}/images/CC_GGM.png" width="300"><br>
<b>(a) CC-GGM [Phi et al., 2024]</b>
</td>

<td align="center">
<img src="{{site.url}}/images/LRCC.png" width="300"><br>
<b>(b) LRCC [Phi et al., 2026]</b>
</td>
</tr>

<tr>
<td align="center">
<img src="{{site.url}}/images/GGFM.png" width="300"><br>
<b>(c) GGFM [Hippert et al., 2023]</b>
</td>

<td align="center">
<img src="{{site.url}}/images/GGM.png" width="300"><br>
<b>(d) GGM [Hippert et al., 2023]</b>
</td>
</tr>

<tr>
<td align="center" colspan="2">
<img src="{{site.url}}/images/SGL.png" width="300"><br>
<b>(e) SGL [Kumar et al., 2020]</b>
</td>
</tr>
</table>

</div>

References
===

<a id="ref-phi2026leveraging"></a>

**Phi, T. H.**, Hippert-Ferrer, A., Bouchard, F., and Breloy, A., *“Leveraging Low-rank Factorizations of Conditional Correlation Matrices in Graph Learning,”* **IEEE Transactions on Signal Processing**, 2026.

<a id="ref-phi2024standardization"></a>

**Phi, T. H.**, Hippert-Ferrer, A., Bouchard, F., and Breloy, A., *“Leveraging Standardization in Graph Learning,”* in **Proceedings of the 32nd European Signal Processing Conference (EUSIPCO)**, 2024, pp. 2492–2496.

<a id="ref-hippert2023learning"></a>

Hippert-Ferrer, A., Bouchard, F., Mian, A., Vayer, T., and Breloy, A., *“Learning Graphical Factor Models with Riemannian Optimization,”* in **Joint European Conference on Machine Learning and Knowledge Discovery in Databases (ECML PKDD)**, Springer, 2023, pp. 349–366.

<a id="ref-kumar2020unified"></a>

Kumar, S., Ying, J., Cardoso, J. V. M., and Palomar, D. P., *“A Unified Framework for Structured Graph Learning via Spectral Constraints,”* **Journal of Machine Learning Research**, vol. 21, no. 22, pp. 1–60, 2020.

<a id="ref-ying2020nonconvex"></a>

Ying, J., de Miranda Cardoso, J. V., and Palomar, D. P., *“Nonconvex Sparse Graph Learning under Laplacian Constrained Graphical Model,”* **Advances in Neural Information Processing Systems (NeurIPS)**, vol. 33, pp. 7101–7113, 2020.

<a id="ref-egilmez2017graph"></a>

Egilmez, H. E., Pavez, E., and Ortega, A., *“Graph Learning from Data under Structural and Laplacian Constraints,”* **IEEE Journal of Selected Topics in Signal Processing**, 2017.

<a id="ref-friedman2008graphical"></a>

Friedman, J., Hastie, T., and Tibshirani, R., *“Sparse Inverse Covariance Estimation with the Graphical Lasso,”* **Biostatistics**, vol. 9, no. 3, pp. 432–441, 2008.

